import styled from 'styled-components';

export const StyledTrendingSearch = styled.a`
  color: #aaa;
  cursor: pointer;
  font-family: sans-serif;
  font-size: 0.75rem;
  font-weight: bold;
  margin-right: 0.5rem;
  padding: 0.15rem;
  text-transform: uppercase;
  transition: 0.2s;

  &:hover {
    color: #fff;
  }
`;
